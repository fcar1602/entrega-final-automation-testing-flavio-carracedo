[pytest]
addopts = -q --strict-markers --html=reportApi.html --self-contained-html
testpaths = preEntrega/tests_api
python_files = test_*.py
filterwarnings = ignore::DeprecationWarning
markers =
    smoke: smoke tests
    exception: tests that verify exceptions
